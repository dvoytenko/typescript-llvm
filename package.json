{
  "name": "stsc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "debug_ast": "npm run build && node ./out/src/debug_ast.js",
    "debug_ast:watch": "nodemon -e ts,json --ignore out --ignore dist --exec \"npm run debug_ast\"",
    "test": "npm run build && node ./out/test/index.js",
    "test:watch": "nodemon -e ts,json --ignore out --ignore dist --exec \"npm run test\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.0.3",
    "@typescript/vfs": "^1.3.5",
    "llvm-bindings": "file:../llvm-bindings",
    "nodemon": "^2.0.19",
    "typescript": "^4.7.4"
  }
}
