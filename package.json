{
  "name": "stsc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "run": "node ./out/src/index.js",
    "build_and_run": "npm run build && npm run run",
    "build_and_run:watch": "nodemon -e ts,json --ignore out --ignore dist --exec \"npm run build_and_run\"",
    "test": "npm run build && node ./out/test/index.js",
    "test:watch": "nodemon -e ts,json --ignore out --ignore dist --exec \"npm run test\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.0.3",
    "@typescript/vfs": "^1.3.5",
    "llvm-bindings": "^0.4.0",
    "nodemon": "^2.0.19",
    "typescript": "^4.7.4"
  }
}
